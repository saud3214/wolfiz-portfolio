// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',
  images: {
    domains: ['images.unsplash.com', 'tympanus.net'],
  },
};

export default nextConfig;
